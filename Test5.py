import math

total = 0

lower_bound = int(input('input lower bound: '))
upper_bound = int(input('input upper bound: '))

for num in range(lower_bound, upper_bound+1, 1):
    total = total + num
    print (total)
    
    

    

    


                  
